A budget calculator. Coded with modular design. A budget controller handles all input and expense logic. A UI controller updates the UI with data from the budget controller and  Global app controller allows for communication between the Budget and UI controllers. A user may input all incomes and expenses for the current month. The App then displays the available funds. App also tracks what percentage of funds are taken up by expenses.